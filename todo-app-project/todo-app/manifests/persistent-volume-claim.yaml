apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: dwk-project
  name: todo-app-claim
spec:
  storageClassName: my-dwk-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
