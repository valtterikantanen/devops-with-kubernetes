apiVersion: v1
kind: Service
metadata:
  namespace: dwk-project
  name: todo-app-backend-svc
spec:
  type: ClusterIP
  selector:
    app: todo-app-backend
  ports:
    - protocol: TCP
      port: 2345
      targetPort: 3000
